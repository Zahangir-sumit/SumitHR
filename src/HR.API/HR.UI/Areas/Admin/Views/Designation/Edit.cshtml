@model HR.MVC.DHK.Models.Designation

@{
    ViewData["Title"] = "Edit Designation";
}
<div class="container">
    <div class="row">
        <div class="col-md-6">
            @using (Html.BeginForm("Edit", "Desigation", FormMethod.Post))
            {
                @Html.AntiForgeryToken()

                <div class="form-group">
                    @Html.HiddenFor(model => model.DesigId) <!-- Add hidden field for DeptId -->
                    <label for="DeptName">Department Name:</label>
                    @Html.TextBoxFor(model => model.DesigName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(model => model.DesigName, "", new { @class = "text-danger" })
                </div>

                @*        <div class="form-group">
            <label for="ComId">Company:</label>
            @Html.DropDownListFor(model => model.ComId, new SelectList(ViewBag.Companies, "ComId", "ComName"), "Select Company", new { @class = "form-control" })
            @Html.ValidationMessageFor(model => model.ComId, "", new { @class = "text-danger" })
            </div>
            *@
                <button type="submit" class="btn btn-primary">Save</button>
                <a href="@Url.Action("Index", "Designation")" class="btn btn-secondary">Cancel</a>
            }
        </div>
    </div>
</div>